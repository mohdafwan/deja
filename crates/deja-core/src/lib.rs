//! deja-core — Déjà ka context engine (DB, snapshot capture, diff/culprit ranking).
//! CLI (`deja`) aur GUI terminal (`deja-term`) dono isse use karte hain.

pub mod db;
pub mod diff;
pub mod snapshot;
